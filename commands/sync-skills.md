---
name: sync-skills
description: ~/.claude/skills를 kmando01/claude-playground 저장소에 동기화한다. 인자 없으면 sync만, "push" 인자 주면 commit+push까지 수행.
---

`sync-skills-repo` 스킬을 실행한다.

- `/sync-skills` → 모드 A (동기화만)
- `/sync-skills push` → 모드 A → 모드 B (동기화 후 commit & push)
